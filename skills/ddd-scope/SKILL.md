---
name: ddd-scope
description: "把模糊的业务诉求收敛成结构化的范围输入：问题陈述、价值主张、目标/非目标、硬约束、通用语言种子、风险。"
risk: safe
category: discovery
inputs: "自然语言业务诉求（可选：业务文档、KPI、合规/团队/时限约束）"
outputs: "范围工件：问题陈述 / 价值主张 / 目标·非目标 / 硬约束 / 通用语言种子 / 风险与开放问题"
tags: "[discovery, scoping]"
---

# Scope（范围收敛）

## 做什么

把一段"我想做个 XX 系统"的模糊愿望，收敛成一份**边界清晰、可被业务方确认**的结构化范围输入。只做范围收敛——不建模、不画事件流。

## 需要什么参数

- **必需**：用户的自然语言诉求（要做什么、给谁用、解决什么痛点）。
- **可选**：已有业务文档、竞品、KPI/OKR、合规要求、团队与时限约束。

> 参数以工件文件形式在调用时给入；本能力不关心它由谁产生。

## 怎么做

1. **抽取问题陈述**：一句话写清"谁、在什么情境、遇到什么问题、现在怎么凑合解决"。拒绝用"功能"代替"问题"（"要一个日历"是功能，"多人抢同一间会议室导致冲突"才是问题）。
2. **明确价值主张**：这个系统对组织/用户的**核心、不可替代价值**是什么。
3. **划定目标 / 非目标**：本期要解决的（目标）与**明确不做的**（非目标）；非目标同等重要，用于堵住范围蔓延。
4. **采集硬约束**：合规、SLA、集成、团队规模、上线时限；区分"硬约束（不可协商）"与"偏好（可协商）"。
5. **播撒通用语言种子**：把用户口中反复出现的业务名词**原样**记下，不翻译成技术词。
6. **登记风险与开放问题**：列出会影响后续、但现在还不确定的点，标注阻塞级别。
7. **回放确认**：用业务语言复述结构化结果，得到确认。

## 返回什么

一份**范围工件**，含下列各段：

| 段 | 结构 |
| :--- | :--- |
| 问题陈述 | 主体 + 情境 + 痛点 + 现状凑合方案 |
| 价值主张 | 1–3 条：为谁创造什么不可替代价值 |
| 目标 / 非目标 | 两栏各 3–7 条 |
| 硬约束 | 约束项、类型（合规/集成/团队/时限）、是否可协商 |
| 通用语言种子 | 业务术语、用户原话定义、疑似歧义标记 |
| 风险与开放问题 | 问题、影响、阻塞级别（阻塞/重要/可延后）|

> **合格产物自检**：问题陈述描述的是"问题"而非"功能"；非目标 ≥ 3 条；术语种子全部来自用户原话、未被提前技术化；所有"阻塞级"开放问题已问清或记为显式假设。

## 示例

```text
输入（诉求）：
  "我们想做个会议室预订系统，老是有人抢同一间房，还要能审批。"

输出（范围工件，节选）：
  问题陈述：员工临时约会议时，因无统一预订入口，多人对同一时段同一间房重复预订
            造成冲突；现状靠 IM 口头协调 + 门口贴纸，经常撞车。
  价值主张：1) 杜绝时段冲突，订到=一定有房；2) 高价值会议室可审批、可追溯。
  目标：冲突检测；预订生命周期（申请/确认/取消）；高价值房审批。
  非目标：访客签到、餐饮联动、跨公司租赁、设备报修（本期不做）。
  硬约束：SSO 接现有 LDAP（集成/不可协商）；8 周上线试点（时限/可小幅协商）。
  通用语言种子：预订、时段、会议室、审批、冲突。
  风险：跨时区预订是否支持？（影响时段建模，阻塞）
```
